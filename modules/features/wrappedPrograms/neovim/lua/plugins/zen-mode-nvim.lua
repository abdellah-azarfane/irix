return {
    "zen-mode.nvim",
    after = function()
        -- require("zen-mode").toggle({
        --   window = {
        --     width = .80 -- 80% of editor width
        --   }
        -- })

    end
}
